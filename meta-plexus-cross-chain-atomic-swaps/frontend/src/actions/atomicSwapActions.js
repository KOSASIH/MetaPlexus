export const initAtomicSwap = () => async (dispatch) => {
    // initialize atomic swap contract instance
};

export const swapTokens = (tokenA, tokenB, amountA, amountB) => async (dispatch) => {
    // call atomic swap contract function
};
